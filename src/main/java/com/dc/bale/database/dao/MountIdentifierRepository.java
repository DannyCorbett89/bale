package com.dc.bale.database.dao;

import com.dc.bale.database.entity.MountIdentifier;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MountIdentifierRepository extends JpaRepository<MountIdentifier, Long> {
}
